Config = {}

Config.Debug = false

-- Opens the cached object's pose list only while the player is using a pose.
Config.StartKey = 0x80F28E95 -- L

Config.MenuScale = {
    Default = 1.0,
    Min = 0.5,
    Max = 2,
    Step = 0.05,
}
